# Pick ONE — Browser-Use will use whichever key is set, in this priority:
# 1. OpenAI (recommended for cost/speed: gpt-4o-mini)
OPENAI_API_KEY=sk-...

# 2. Anthropic (fallback)
# ANTHROPIC_API_KEY=sk-ant-...

# 3. Google Gemini (fallback)
# GOOGLE_API_KEY=...
